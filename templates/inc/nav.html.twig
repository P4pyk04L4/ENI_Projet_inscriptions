<div class=" m-3">
    <div class="text-white d-flex flex-column" style="height: 100px">

        <nav class="navbar navbar-dark navbar-expand-lg">
            <div class="container-fluid mx-4 ">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        {% if app.user.administrateur %}
                            <li class="nav-item">
                                <a class="nav-link" href="#">Villes</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">Campus</a>
                            </li>
                        {% endif %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('sortie_index') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Mon profil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Se déconnecter</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="align-self-end me-5">
            <div>
                <strong>Date du jour : {{ "now"|date('d/m/Y') }}</strong>
            </div>
            <div>
                <strong>Participant : {{ app.user.prenom }} {{ app.user.nom|first }}.</strong>
            </div>
        </div>
    </div>
</div>

